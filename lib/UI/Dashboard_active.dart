import 'package:flutter/material.dart';

class DashboardActive extends StatelessWidget {
  const DashboardActive({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
